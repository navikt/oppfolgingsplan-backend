name: Copilot Config Sync
on:
  schedule:
    - cron: '0 5 * * *'
  workflow_dispatch:
jobs:
  sync:
    uses: navikt/copilot-kitchen/.github/workflows/copilot-sync.yml@main
    with:
      collections: "esyfo,backend"
    secrets:
      APP_PRIVATE_KEY: ${{ secrets.AUTOMERGE_APP_PRIVATE_KEY }}
    permissions:
      contents: write
      pull-requests: write
